package com.sparta.studytrek.domain.like.service;

import com.sparta.studytrek.domain.auth.entity.User;

public class ReviewLikeService {

    public int reviewLike(Long campId, Long reviewId, User user) {
        return 0;
    }
}
